FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Generate Prisma client
RUN npx prisma generate

# Push schema to DB (creates tables without migrations; use 'migrate dev' for dev with history)
RUN npx prisma db push --accept-data-loss

EXPOSE 3000

CMD ["npm", "start"]